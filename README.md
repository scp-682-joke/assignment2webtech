# Assignment 2 — The Game Shelf

HTML/CSS only: Flexbox game cards, a Grid profile and gallery, and a portfolio combining both.

Open `index.html` and use the navbar to visit all four tasks. No installation needed.

For GitHub Pages, publish the repository root (`/`); `index.html` is the homepage.
